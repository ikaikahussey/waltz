{
  "name": "waltz",
  "version": "0.1.0",
  "type": "module",
  "description": "Multi-campaign volunteer platform wrapping the walk + match tools.",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "lint": "eslint .",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.5.0",
    "dotenv": "^16.4.5",
    "hono": "^4.6.10"
  },
  "devDependencies": {
    "eslint": "^9.15.0"
  }
}
