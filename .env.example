# Copy to .env and adjust.
PORT=3000
DATABASE_PATH=./data/waltz.db
# Set in production. Used to mark session cookies Secure and build invite URLs.
PUBLIC_ORIGIN=http://localhost:3000
NODE_ENV=development
